# Agentless Monitor – TODO

## Features

- [x] OTP Application scaffold (`mix.exs`, `application.ex`)
- [x] Configuration loading from Application env and `config.json` (`config.ex`)
- [x] Runtime configuration via environment variables (`config/runtime.exs`)
- [x] Data models for all entities (`models.ex`)
- [x] GenServer state management with full CRUD (`state.ex`)
- [x] SSH config parsing from `~/.ssh/config` or `SSH_CONFIG_PATH`
- [x] Local "localhost" server added automatically on startup
- [x] SSH connection execution via `ssh` CLI (`ssh/connection.ex`)
- [x] SSH multiplexing via `ControlMaster` / `ControlPersist`
- [x] Password-based SSH via `sshpass` (`ssh/connection.ex`)
- [x] SSH connection registry GenServer (`ssh/manager.ex`)
- [x] Monitoring mega-command for collecting all metrics in one SSH call
- [x] Local monitoring by reading `/proc/*` directly
- [x] CPU usage parsing from `/proc/stat`
- [x] Memory info parsing from `/proc/meminfo`
- [x] Disk info parsing from `df -h` output
- [x] Network interface parsing from `/proc/net/dev`
- [x] Listening ports parsing from `ss -tuln` / `netstat -tuln`
- [x] Ping tests to 8.8.8.8 and 1.1.1.1
- [x] System info (hostname, OS, kernel, arch, uptime)
- [x] Monitoring history with max 1000 entries per server (`state.ex`)
- [x] Background polling loop every second (`monitoring/service.ex`)
- [x] Task.Supervisor for async monitoring tasks
- [x] Monitoring job tracking (created, running, completed, failed, cancelled)
- [x] REST API with Plug.Router (`api/router.ex`)
- [x] `GET /api/health`
- [x] `GET /api/servers`
- [x] `GET /api/servers/:id`
- [x] `GET /api/servers/:id/status`
- [x] `GET /api/servers/:id/details/:metric`
- [x] `GET /api/servers/:id/history`
- [x] `POST /api/servers/:id/connect`
- [x] `POST /api/servers/:id/start-monitoring`
- [x] `POST /api/servers/:id/stop-monitoring`
- [x] `GET /api/jobs`
- [x] `GET /api/jobs/statistics`
- [x] `POST /api/jobs/:id/cancel`
- [x] `POST /api/jobs/clear`
- [x] `POST /api/monitoring/pause-all`
- [x] `POST /api/monitoring/resume-all`
- [x] `GET /api/connection-stats`
- [x] `GET /api/connection-pool`
- [x] `GET /api/config-info`
- [x] `GET /` serves `templates/index.html`
- [x] `GET /static/*` serves static files
- [x] JSON encoding via Jason (all structs converted to plain maps)
- [x] UUID generation using `:crypto`
